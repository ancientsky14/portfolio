node_modules/
.next/
out/
build/
.vercel
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# local verification screenshots (headless Edge) — never committed
tmp-shots/

# uncut tour recordings kept by scripts/media/reencode.mjs — the short cut ships
public/work/*/*.full.webm

# Playwright MCP session output (page snapshots, console logs)
.playwright-mcp/

# visits Worker — local D1 state and local secrets
workers/visits/.wrangler/
workers/visits/.dev.vars

# the site's own Worker (root wrangler.jsonc) — local dev state
/.wrangler/

# contact Worker — local D1 state and local secrets
workers/contact/.wrangler/
workers/contact/.dev.vars
docs/
PHASE-1.md
PHASE-2.md
PHASE-3.md
PLAN-V2.md
