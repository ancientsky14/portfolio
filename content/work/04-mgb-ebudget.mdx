---
slug: mgb-ebudget
title: "eBudget"
fullName: "MGB RO1 eBudget & Accounting System" # the repo's own title (README)
subtitle: "Desktop budgeting and accounting, with a self-updating installer"
client: "Mines and Geosciences Bureau — Regional Office I"
clientCleared: true # cleared for naming by Jan, 2026-09-11
clientAnonymous: "A national bureau's regional office"
sector: "Finance and accounting"
platform: "Desktop"
status: "In testing" # Jan, 2026-09-11
version: "1.0.11"
year: 2026
role: "Sole developer — design, build, releases, maintenance"
stack: ["Tauri 2", "Rust", "React 19", "TypeScript", "SQLite", "PostgreSQL"]
repo: "ancientsky14/mgb-ebudget"
repoPublic: false
# A desktop app has no web demo. Its live surface is the public release feed
# every installed copy updates from.
live:
  - kind: "release"
    label: "Release feed"
    href: "https://github.com/ancientsky14/mgb-ebudget-releases/releases/latest"
    note: "Signed installers — the app updates itself from here"
    # Unlinked by Jan, 2026-09-17: no GitHub links on the site (R20). The
    # repo itself stays public — every installed copy updates from it.
    cleared: false
previewNote: "Recorded on a development build with fictional FY 2026 records — never the office's own data."
featured: true
order: 1
bodyReviewed: true # body confirmed by Jan, 2026-09-11
summary: >-
  A Windows desktop app for budget execution and accounting — allotments,
  obligation requests, disbursement vouchers, financial reports, journals and
  ledgers. Several PCs in the office share one database, and the app updates
  itself from signed releases. Built on SQLite first, later migrated to
  Postgres.
# Only the number that is certain: the PC count is not published (Jan, 2026-09-11).
metrics:
  - label: "Current release"
    value: "v1.0.11"
---

{/*
  ────────────────────────────────────────────────────────────────
  REVIEWED — body confirmed by Jan, 2026-09-11

  CONFIRMED (read from the mgb-ebudget repo):
    · Windows desktop app: Tauri 2 + React 19 + Rust
    · UACS budget execution and NGA accounting, aligned with the GAM for NGAs
      and the UACS Manual
    · allotments (GAARD / SARO / Sub-ARO), obligation requests with an
      over-obligation guard, disbursement vouchers capped at the obligated
      balance, FAR reports, journals and ledgers
    · audit logging on reference data; PDF (react-pdf) and Excel (exceljs)
    · signed installer and in-app updater; v1.0.11
    · migrated from SQLite to Postgres on Supabase on 2026-08-30, with
      verified backups and a data migration

  CONFIRMED by Jan, 2026-09-11:
    · status: in testing (not yet in daily use)
    · the move to Postgres was for remote access and managed backups; it
      gave up offline use — never claim offline use for eBudget
    · every PC connects to the Supabase database directly; the old "pair
      with a code" LAN sharing is gone. The hosting PC still serves the
      installer over the office network.
    · MGB RO1 may be named. No PC count published; no "what I'd do
      differently" section.
  ────────────────────────────────────────────────────────────────
*/}

## What it is

A desktop app for the budget and accounting side of an office: every peso from
allotment to obligation to disbursement, and the reports that show where it
went.

## What I built

- **A desktop app, not a browser tab.** Tauri 2 with a Rust backend and a React
  19 interface — one installer, no browser to keep open.
- **Money that cannot overdraw.** Obligation requests carry an over-obligation
  guard, and disbursement vouchers are capped at what was obligated. Every
  document moves through a fixed lifecycle — draft, then obligated or
  certified, then paid or cancelled.
- **One database, several desks.** Every PC connects to the same Postgres
  database, so everyone works from the same records.
- **Updates that install themselves.** Releases are signed; the app checks on
  launch and offers the new version. The hosting PC also serves the installer
  to the rest of the office network.
- **Changes on record.** An audit log on every change to reference data.
- **Reports the office already files,** out as PDF and Excel.

### The database move

Built on SQLite first, then migrated to Postgres on Supabase — with verified
backups and a real data migration, not a fresh start. The move brought access
to the data from outside the office network and backups the platform manages.
It gave up offline use.

## Status

In testing with the office, at v1.0.11.